import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

/** Configuración — gestión de categorías (Fase 3) y selección de tema (Fase 7). */
export default function SettingsScreen() {
  return <PlaceholderScreen title="Configuración" />;
}
