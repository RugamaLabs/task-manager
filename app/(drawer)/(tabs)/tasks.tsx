import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

/** Tasks — tareas agrupadas por categoría. Se construye en la Fase 3. */
export default function TasksScreen() {
  return <PlaceholderScreen title="Tasks" />;
}
