import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

/** Habits — seguimiento de hábitos con racha semanal. Se construye en la Fase 4. */
export default function HabitsScreen() {
  return <PlaceholderScreen title="Habits" />;
}
